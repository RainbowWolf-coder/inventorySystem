rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default: deny all direct client access.
    // This project currently uses Cloud Functions (Admin SDK) which bypasses rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
